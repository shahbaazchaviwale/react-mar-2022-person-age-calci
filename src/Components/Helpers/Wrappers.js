/** ************************************************ */
/** This function used for to avoid the unnecessary <div> tags called as wrapper  */
/** ************************************************ */

const Wrappers = (props) => {
    return props.children;
}
export default Wrappers;